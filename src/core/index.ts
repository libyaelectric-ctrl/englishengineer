/**
 * Core barrel — intentional placeholder.
 *
 * Core modules are consumed directly (e.g. `@/core/learning`).
 * This file exists only to satisfy the architecture test that
 * verifies no unused exports leak from core.
 */
export {};
