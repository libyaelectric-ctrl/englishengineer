// TEMPORARY CI probe — proves 🔍 Code Quality fails on a raw palette class.
// Removed in the next commit; never merged.
export function PaletteCiProbe() {
  return <div className="bg-slate-900 text-white">ci probe</div>;
}
