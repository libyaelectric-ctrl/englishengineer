import { FirebaseError } from 'firebase/app';
import { ArrowLeft, LogIn, Moon, Sun, UserCheck } from 'lucide-react';

import { useEffect, useState } from 'react';
import { Link, useLocation, useNavigate } from 'react-router-dom';

import { PRODUCT_VERSION } from '@/config/product.config';
import { useAuthStore } from '@/features/auth';
import { useFirebaseAuth } from '@/features/auth/FirebaseAuth';
import { AUTH_SIGN_IN_FALLBACK_REDIRECT_URL, AUTH_SIGN_IN_URL, AUTH_SIGN_UP_FALLBACK_REDIRECT_URL, AUTH_SIGN_UP_URL } from '@/features/auth/firebase.config';
import { useTheme } from '@/features/theme/ThemeProvider';

type AuthPageProps = { mode: 'sign-in' | 'sign-up' };
type AuthLocationState = { from?: { pathname?: string } } | null;
const getReturnTarget = (search: string, state: AuthLocationState): string | undefined => { const redirectUrl = new URLSearchParams(search).get('redirect_url'); if (redirectUrl?.startsWith('/')) return redirectUrl; const fromPath = state?.from?.pathname; if (fromPath && fromPath !== AUTH_SIGN_IN_URL && fromPath !== AUTH_SIGN_UP_URL) return fromPath; return undefined; };
const describeAuthError = (error: unknown): string => error instanceof FirebaseError ? (error.code === 'auth/email-already-in-use' ? 'Bu e-posta zaten kayıtlı. Giriş yapmayı deneyin.' : error.code === 'auth/weak-password' ? 'Şifre en az 6 karakter olmalı.' : error.code === 'auth/network-request-failed' ? 'Ağ bağlantısı kurulamadı. İnternetinizi kontrol edin.' : 'E-posta veya şifre hatalı.') : error instanceof Error ? error.message : 'Giriş başarısız oldu.';
const describeGoogleAuthError = (error: unknown): string => error instanceof FirebaseError ? (error.code === 'auth/popup-closed-by-user' ? 'Google penceresi kapatıldı. Tekrar deneyin.' : error.code === 'auth/popup-blocked' ? 'Tarayıcı Google penceresini engelledi. Yönlendirme deneniyor…' : `Google girişi başarısız oldu (${error.code}).`) : error instanceof Error ? error.message : 'Google girişi başarısız oldu.';
const GOOGLE_REDIRECT_FALLBACK_CODES = new Set(['auth/popup-blocked', 'auth/invalid-credential', 'auth/internal-error', 'auth/network-request-failed']);
const GoogleButton = ({ busy, onBusyChange }: { busy: boolean; onBusyChange: (b: boolean) => void }) => { const { signInWithGoogle, signInWithGoogleRedirect } = useFirebaseAuth(); const [error, setError] = useState<string | null>(null); const handleClick = async () => { onBusyChange(true); setError(null); try { await signInWithGoogle(); } catch (err) { const code = err instanceof FirebaseError ? err.code : null; if (code && GOOGLE_REDIRECT_FALLBACK_CODES.has(code)) { try { setError(describeGoogleAuthError(err)); await signInWithGoogleRedirect(); return; } catch (redirectErr) { setError(describeGoogleAuthError(redirectErr)); onBusyChange(false); return; } } setError(describeGoogleAuthError(err)); onBusyChange(false); } }; return <div><button type="button" onClick={() => void handleClick()} disabled={busy} className="flex w-full items-center justify-center gap-3 rounded-2xl border border-slate-200 bg-white px-4 py-3 text-base font-black text-slate-800 shadow-sm transition hover:bg-slate-50 disabled:opacity-60 dark:border-white/10 dark:bg-white/[0.08] dark:text-white dark:hover:bg-white/[0.12]">Google ile devam et</button>{error && <p className="mt-2 text-sm text-red-600 dark:text-red-300">{error}</p>}</div>; };
const EmailPasswordForm = ({ mode, busy, onBusyChange }: { mode: 'sign-in' | 'sign-up'; busy: boolean; onBusyChange: (b: boolean) => void }) => { const { signInWithEmail, signUpWithEmail } = useFirebaseAuth(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [name, setName] = useState(''); const [error, setError] = useState<string | null>(null); const handleSubmit = async (event: React.FormEvent) => { event.preventDefault(); onBusyChange(true); setError(null); try { if (mode === 'sign-in') await signInWithEmail(email.trim(), password); else await signUpWithEmail(email.trim(), password, name.trim() || undefined); } catch (err) { setError(describeAuthError(err)); onBusyChange(false); } }; const inputClass = 'w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-base text-slate-950 outline-none transition placeholder:text-slate-400 focus:border-cyan-500 dark:border-white/10 dark:bg-white/[0.08] dark:text-white'; return <form onSubmit={(e) => void handleSubmit(e)} className="space-y-3">{mode === 'sign-up' && <input type="text" value={name} onChange={(e) => setName(e.target.value)} placeholder="Ad Soyad (isteğe bağlı)" className={inputClass} />}<input type="email" required value={email} onChange={(e) => setEmail(e.target.value)} placeholder="ornek@engvox.com" className={inputClass} /><input type="password" required minLength={6} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Şifre (en az 6 karakter)" className={inputClass} />{error && <p className="text-sm text-red-600 dark:text-red-300">{error}</p>}<button type="submit" disabled={busy} className="flex w-full items-center justify-center gap-2 rounded-2xl bg-slate-950 px-4 py-3 text-base font-black text-white transition hover:-translate-y-0.5 disabled:opacity-60 dark:bg-white dark:text-slate-950">{mode === 'sign-in' ? <LogIn className="h-5 w-5" /> : <UserCheck className="h-5 w-5" />}<span>{busy ? 'İşleniyor…' : mode === 'sign-in' ? 'Giriş yap' : 'Hesap oluştur'}</span></button></form>; };
const AuthPage = ({ mode }: AuthPageProps) => { const location = useLocation(); const navigate = useNavigate(); const { isSignedIn } = useFirebaseAuth(); const { theme, toggleTheme } = useTheme(); const returnTarget = getReturnTarget(location.search, location.state as AuthLocationState); const targetDestination = mode === 'sign-in' ? returnTarget ?? AUTH_SIGN_IN_FALLBACK_REDIRECT_URL : returnTarget ?? AUTH_SIGN_UP_FALLBACK_REDIRECT_URL; const [showAccountForm, setShowAccountForm] = useState(false); const [authBusy, setAuthBusy] = useState(false); useEffect(() => { if (isSignedIn) navigate(targetDestination, { replace: true }); }, [isSignedIn, navigate, targetDestination]); const handleQuickDemoStart = () => { useAuthStore.getState().enterDemoUser(); navigate(targetDestination, { replace: true }); };
  return <div className="fixed inset-0 z-50 overflow-hidden bg-[#f7f9fc] text-slate-950 dark:bg-[#040611] dark:text-white"><div className="pointer-events-none absolute inset-0 bg-[linear-gradient(180deg,#ffffff_0%,#f7f9fc_52%,#eef4f8_100%)] dark:bg-[linear-gradient(180deg,#040611_0%,#070b18_100%)]" /><div className="pointer-events-none absolute -left-32 top-1/4 h-80 w-80 rounded-full bg-cyan-200/15 blur-3xl" /><div className="pointer-events-none absolute -right-32 top-10 h-[22rem] w-[22rem] rounded-full bg-indigo-200/12 blur-3xl" /><div className="pointer-events-none absolute bottom-1/4 left-1/3 h-64 w-64 rounded-full bg-amber-100/15 blur-3xl" /><div className="relative flex h-full flex-col items-center justify-center gap-4 px-4 py-4"><div className="flex w-full max-w-md shrink-0 items-center justify-between"><Link to="/" className="inline-flex items-center gap-1.5 text-base font-bold text-slate-600 transition hover:text-slate-950 dark:text-white/65 dark:hover:text-white"><ArrowLeft className="h-5 w-5" />Ana sayfa</Link><div className="flex items-center gap-2"><button type="button" onClick={toggleTheme} className="grid h-10 w-10 place-items-center rounded-2xl border border-slate-200 bg-white text-slate-700 shadow-sm dark:border-white/10 dark:bg-white/[0.08] dark:text-white" aria-label="Tema değiştir">{theme === 'dark' ? <Sun className="h-4 w-4 text-amber-300" /> : <Moon className="h-4 w-4 text-cyan-700" />}</button><img src="/brand/logo.svg" alt="EngVox" className="h-9 w-9 rounded-xl" /><span className="text-xs font-black text-cyan-700 dark:text-cyan-100">v{PRODUCT_VERSION}</span></div></div><div className="w-full max-w-md shrink-0 rounded-[2rem] border border-slate-200 bg-white p-7 shadow-xl dark:border-white/10 dark:bg-[#0c1425]/80"><h1 className="text-3xl font-black tracking-tight text-slate-950 dark:text-white">{mode === 'sign-in' ? 'Giriş yap' : 'Ücretsiz başlayın'}</h1><p className="mt-2 text-base font-semibold leading-7 text-slate-700 dark:text-slate-200">{mode === 'sign-in' ? 'Mühendislik İngilizcesi çalışmalarına kaldığın yerden devam et.' : 'Hesabını oluştur, ilerlemen güvenle kaydedilsin.'}</p><div className="mt-6"><GoogleButton busy={authBusy} onBusyChange={setAuthBusy} /></div><div className="my-6 flex items-center gap-3"><div className="h-px flex-1 bg-slate-200 dark:bg-white/10" /><span className="text-sm font-bold text-slate-500 dark:text-white/50">veya</span><div className="h-px flex-1 bg-slate-200 dark:bg-white/10" /></div>{!showAccountForm ? <button type="button" onClick={() => setShowAccountForm(true)} className="flex w-full items-center justify-center rounded-2xl border border-slate-200 bg-white py-3 text-base font-black text-slate-700 transition hover:bg-slate-50 dark:border-white/10 dark:bg-white/[0.05] dark:text-white/70 dark:hover:text-white">E-posta ile devam et</button> : <EmailPasswordForm mode={mode} busy={authBusy} onBusyChange={setAuthBusy} />}<p className="mt-5 text-base text-slate-700 dark:text-slate-200">{mode === 'sign-in' ? <>Hesabınız yok mu? <Link to={AUTH_SIGN_UP_URL} className="font-black text-cyan-700 hover:underline dark:text-cyan-100">Kayıt olun</Link></> : <>Zaten hesabınız var mı? <Link to={AUTH_SIGN_IN_URL} className="font-black text-cyan-700 hover:underline dark:text-cyan-100">Giriş yapın</Link></>}</p><div className="mt-6 flex items-center justify-between border-t border-slate-200 pt-5 dark:border-white/10"><p className="text-sm text-slate-600 dark:text-white/55">Sadece denemek mi istiyorsun?</p><button type="button" onClick={handleQuickDemoStart} className="text-sm font-black text-cyan-700 hover:underline dark:text-cyan-100">Demo ile devam et</button></div></div><p className="max-w-md text-center text-xs font-semibold text-slate-500 dark:text-white/45">Verileriniz güvenli şekilde korunur.</p></div></div>;
};
export default AuthPage;
